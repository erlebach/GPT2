model:
  n_layer: 4
  n_head: 8
  n_embd: 1024
  n_blocks_per_super: 2

# Note: This simplified format just overrides model parameters
# The optimizer is still created using the original hardcoded approach
