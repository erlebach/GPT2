# GPT2
Reference implementation of GPT2 with Pytorch. Base for further experiments. 

# ./src is in sys.path: 
Check out ` cat .venv/lib/python3.10/site-packages/__editable__.nanogpt-0.1.0.pth`, which contains: 
```text
/Users/erlebach/src/2025/GPT2/src
```
which is automatically generated with 'uv sync' if `pyproject.toml` has `[project]` with the fields `name`, 
`version`, and `requires-python`. 
